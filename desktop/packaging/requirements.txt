# What the offline studio's build needs, beyond the API's own requirements.txt.
#
# Pinned to majors rather than exact versions: these are build tools, and a
# patch release of PyInstaller that fixes a platform hook is one we want.
fastapi>=0.115
uvicorn>=0.30
pywebview>=5.0
pyinstaller>=6.10
# tools/adapter_to_gguf.py and desktop/packaging/make_icons.py
gguf>=0.10
numpy>=1.26
pillow>=10.0
